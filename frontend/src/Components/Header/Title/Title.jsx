import React from 'react'
import style from "./Title.css"

function Title({ title }) {
    return (
        <div class="Title">
            <span>{title}</span>
        </div>
    )
}

export default Title
